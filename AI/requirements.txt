--extra-index-url https://download.pytorch.org/whl/cpu

# ==========================================
# CORE AI LIBRARIES (Mô hình và tính toán)
# ==========================================
torch>=2.0.0
transformers>=4.30.0

# ==========================================
# IMAGE PROCESSING (Xử lý ảnh)
# ==========================================
Pillow>=9.0.0

# ==========================================
# OCR MODULE (Trích xuất văn bản)
# ==========================================
easyocr>=1.7.0
# Fast CPU-first OCR path. EasyOCR remains installed as the quality fallback.
rapidocr==3.9.1
onnxruntime==1.19.2

# ==========================================
# FIX CONFLICT (Bản vá lỗi xung đột phiên bản)
# ==========================================
# Cố định bản numpy 1.x để tránh lỗi _ARRAY_API not found với các thư viện cũ
numpy<2

# ==========================================
# DATABASE CONNECTOR (Cho script Indexing)
# ==========================================
qdrant-client

psycopg2-binary==2.9.9
fastapi
uvicorn
python-multipart

requests==2.31.0
pandas==2.2.2

deep-translator
